# Inventory Manager — Frontend

React + Vite single-page app for the stock management system. Talks to the Express API in `../src` (proxied via `/api` in dev, see `vite.config.js`).

## Development

```bash
npm install
npm run dev
```

Requires the backend API running on `http://localhost:3000` (see the repo root `README.md`).
