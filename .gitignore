node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# SQLite dev database
/dev.db
/*.db
/*.db-journal

# Uploaded product images (served from disk, not version controlled)
/uploads/*
!/uploads/.gitkeep
